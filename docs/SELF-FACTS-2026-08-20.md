# Self-facts — trading-system, 2026-08-20

Read-only fact sheet. Every claim below was produced by reading the cited file at the
cited line (or class/method) in `/home/anushadudekula71/trading-system`. Nothing was run.
Three research passes (one per axis) read the files listed in the task; findings are
merged here without editorializing beyond what the code states.

Two engines exist and are cited throughout: **(A)** `src/paper/forward_engine.py` —
replays archived parquet data through `fill_model`/`order_intent_wal`, no network.
**(B)** `src/segment/live_engine.py` — trades on live market data but also never sends
an order to a venue ("this is a paper broker" per its own docstring). They do **not**
share fill-code implementation (Axis 1, row 16) despite (B)'s docstring claiming it uses
`fill_model`.

## Axis 1 — paper-trading mechanics

| mechanism | what the code does | evidence |
|---|---|---|
| Fill price | (A) both an optimistic and pessimistic fill computed per trade, never blended: limit optimistic = resting price (maker), pessimistic = `max(limit,best_ask)`/`min(limit,best_bid)` (taker); market order optimistic = print price, pessimistic = far touch, both taker. (B) single price: `frame["ask"]` long / `frame["bid"]` short, always taker. | `src/paper/fill_model.py:106-222`; `src/segment/live_engine.py:672-675` |
| Slippage | No bps/parameter model. `cost.spread_and_depth.impact_bps` walks the book but is **not imported** by paper/segment. `cost.round_trip_cost.quote_round_trip_cost` hardcodes `spread_bps=0`, `impact_bps=0`, `verified=False`. Stop-exit gap-beyond-bar slippage explicitly not modelled (`slippage_beyond_bar_modelled=False`). | `src/cost/round_trip_cost.py:107-171`; `src/paper/exit_manager.py:132-139` |
| Fees | `position_book.apply_fill`: `fee = qty*price*(maker_bps or taker_bps)/10000`, refuses if venue has no schedule. Hardcoded rates in `forward_engine.VERIFIED_FEES`: binance 2.0/5.0 bps, hyperliquid 1.5/4.5 bps, labelled "Verified 2026-08-09". `cost/fee_schedule.py` labels the *same* binance numbers `DECLARED` (unverified — 401 without a key), contradicting the "VERIFIED" label. Live fetcher exists (`cost/fee_fetcher.py`) but unused by the paper path. | `src/paper/position_book.py:125-153`; `src/paper/forward_engine.py:64-69`; `src/cost/fee_schedule.py:86-104` |
| Funding/carry | `cost/funding_carry.py`: bps from archived settlement rates; binance settles `(0,8,16)` UTC (mark price), hyperliquid hourly `range(24)` (oracle, 4%/hr cap documented not enforced), binance-spot none. **Not wired** into paper_broker/forward_engine/exit_manager (no importer found). | `src/cost/funding_carry.py:39-43,88-103` |
| Partial fills | Supported: fill model caps each fill by `min(remaining, available)`, loops over multiple print events. `execution.order_lifecycle.Order` tracks by remaining quantity: `PENDING→PARTIAL→FILLED`. | `src/paper/fill_model.py:161-222`; `src/execution/order_lifecycle.py:50-64,111-163` |
| Latency sim | None. Look-ahead prevented instead by event ordering: fills applied to resting orders before new intents from the same print are submitted, so an order can't fill on the bar that created it. | `src/paper/forward_engine.py:335-383`; `src/paper/fill_model.py:154-159` |
| Queue position | Not modelled live. Approximated only for resting **limit** orders by a scalar participation fraction (`available = trade_qty * fraction`); fraction from CLI flag or `participation_calibration.calibrate_participation` (backward-looking median of `max(0,volume-deeper_touch)/volume`). Market orders (the ones actually used) ignore participation entirely. | `src/paper/fill_model.py:44-56,208-209`; `src/paper/participation_calibration.py:61-97` |
| Liquidation | Not simulated as an event/fill. `segment/leverage_policy.liquidation_distance` = `1/leverage - maintenance_margin_rate`, used only to size leverage / fit the stop inside that headroom at trade-open — never force-closes a live position. | `src/segment/leverage_policy.py:156-208` |
| Order state machine | `OrderStatus`: `PENDING, PARTIAL, FILLED, CANCELLED, REJECTED` (last three terminal); `fill()`/`cancel()`/`reject()` enforce legal transitions, raise `InvalidTransition` otherwise. WAL layer separately tracks `submitted/expired/transport_failed/acknowledged/resolved`. | `src/execution/order_lifecycle.py:50-65,144-182`; `src/execution/order_intent_wal.py:245-294` |
| Idempotency/WAL | `OrderIntentWal.submit` fsync-appends intent (`outcome:"unknown"`) **before** calling transport. `derive_client_order_id` = SHA-256 over all distinguishing fields, truncated to Binance's 36-char cap. Re-submitting same intent raises `RuntimeError` (no blind retry). `unresolved()` surfaces crash-orphaned intents for startup reconciliation. | `src/execution/order_intent_wal.py:111-124,197-294` |
| Position sizing + leverage | (A) fixed CLI `--quantity` (default `0.001`), no leverage/capital logic. (B) `live_engine._size_entry`: leverage via `leverage_policy.choose_leverage` (`volatility_targeted`: `target_annual_vol_pct / instrument_ann_vol`, floored/ceilinged, rounded down; or `fixed`: segment ceiling), then `fit_leverage_to_stop` reduces until stop fits `liquidation_distance*0.8` or refuses (`STOP_OUTSIDE_LIQUIDATION`). Sized at `declaration.max_margin_per_trade_usdt` always: `notional=margin*leverage`, `qty=notional/(price*usd_rate)`. Margin reserved from cross-process `CapitalPool` (flock-guarded, ledger-replayed) before fill is journalled. | `src/paper/forward_engine.py:76-99`; `src/segment/leverage_policy.py:113-208`; `src/segment/live_engine.py:567-666`; `src/segment/capital_pool.py:230-277` |
| Stop/target | `strategy/deterministic_exit.run_exit_policy`, 4 rails in precedence: hard stop `entry±3×ATR` (fixed); profit lock — ratchet 1.5×ATR behind best price once armed at 2×ATR move, vs 2×ATR trail, tighter wins; profit target `entry±4×ATR`; vertical barrier `max_holding_bars=480`. ATR = 14-period simple mean of true range, measured once at entry. Same-bar stop+target touch resolves against the position (`is_ambiguous`). All exits fire as market orders off the pessimistic entry price. | `src/strategy/deterministic_exit.py:117-139,282-380`; `src/paper/exit_manager.py:33-70,265-302` |
| Peak excursion | Tracked: `max_favourable`/`max_adverse` accumulated bar-by-bar through the exit walk, ride every `ExitRecord`, stated as the PROFIT-TAIL training feature set. | `src/strategy/deterministic_exit.py:181-225,317-323` |
| Capital declaration per segment | Single file `~/capture/segment/capital.json`, whole-or-nothing parse (`Decimal` only, floats rejected). Fields: `portfolio_usdt`, `per_bot_cap_fraction` (bot cap = product), `min/max_margin_per_trade_usdt`, `leverage.rule/floor/ceiling`, `target_annual_vol_pct`, `maintenance_margin_rate`. Liquidation-distance sanity check rejects a ceiling that would liquidate at open. Reload by mtime; open positions keep their original terms. `GOVERNED_SEGMENTS=("perp","spot")` only; dated/options excluded (RL-039), fixed-quantity legacy path. Not read by `forward_engine` at all. | `src/segment/capital_declaration.py:65,106-165,187-399` |
| USDT P&L accounting | (A) `position_book`: realized P&L per fill in quote currency, two parallel OPTIMISTIC/PESSIMISTIC books, never averaged, fees realized at fill time. (B) `segment/capital_accounting.account_for_fills`: converts non-USD quote via journalled per-fill `usd_rate` (unconvertible fills flagged, never dropped/defaulted); reports 3 never-blended figures — peak-at-risk, turnover, bankroll/equity (`SEGMENT_BANKROLLS_USDT=1000`/bot); all labelled "gross of fees" (no net P&L journal yet). | `src/paper/position_book.py:152-186`; `src/segment/capital_accounting.py:68-78,206-266` |
| Paper-vs-live parity | **No live order submission exists anywhere read.** Engine (A) and (B) do not share fill-code implementation: (B)'s docstring claims it uses `paper.fill_model` but grep found no import/call of `fill_model` in `live_engine.py`, and no import of `order_intent_wal` under `src/segment`. They share only the exit-policy module and cost/leverage helpers by name. `paper_broker.py`'s claim that paper "exercises the code a live transport would run" is contradicted by this. | `src/paper/paper_broker.py:1-38`; `src/segment/live_engine.py:1-45,672-675` |

**NOT PRESENT** (searched, not found):
- Real venue order submission / API credentials in the paper path — searched `src/paper/*`, `src/execution/*` for network/order calls; both engines' docstrings state no order reaches a venue.
- Latency/delay simulation — grepped `latency|delay` in `src/paper`, `src/execution`.
- Liquidation as a triggered fill/event — grepped `liquidat` across `src/paper`, `src/execution`, `src/cost`.
- `cost.spread_and_depth` / `cost.round_trip_cost` / `cost.funding_carry` wired into any actual fill — grepped importers under `src/paper`, `src/segment`; none found (all three are unused by the live fill path).
- Real order-book queue-position tracking at fill time — no live depth consulted per-fill in either engine.
- `capital_declaration`/`capital_pool`/`leverage_policy` used by `forward_engine.py` — grepped its imports; none of the three modules appear.

## Axis 2 — intelligence and learning

| mechanism | what the code does | evidence |
|---|---|---|
| Brains | BULL/BEAR/PROFIT-TAIL, fixed structure whether rule-based or learned. BULL and BEAR **share one LightGBM model** (P(long from here profits under the deterministic exit)); BEAR reads it in reverse with a disclosed `REVERSED_READING_PENALTY=0.05` stricter bar. PROFIT-TAIL times entry/manages position but cannot reject a selected trade (`reject()` always raises `ProfitTailOverreach`). `segment.arbiter.select` picks between BULL/BEAR — explicitly "not a third brain" — requires margin ≥ `0.10` and confidence ≥ `0.55`, else `ABSTAIN`. | `src/learn/learned_brains.py:248-379`; `src/segment/brain.py:1-38,118-149`; `src/segment/profit_tail.py:435-446`; `src/segment/arbiter.py:85-153` |
| Model types | **LightGBM only** (`import lightgbm as lgb`): `binary` objective for direction, `quantile` (0.1/0.5/0.9) for PROFIT-TAIL forward-P&L. No xgboost/PyTorch/TF. `models/stacked_ensemble.py` (ridge-logistic IRLS meta-learner) and `models/ledger_meta_model.py` (regime×family ridge-logistic, `MIN_TRIALS_TO_FIT=120`) exist but are **not called** from the actual training path, which fits one LightGBM booster per segment. | `src/models/gradient_boosted_trees.py:107-140`; `src/learn/train_segment_model.py:159-306`; `src/models/stacked_ensemble.py:1-97`; `src/models/ledger_meta_model.py:71-86` |
| Features | Wired into training: 14 hand-computed features from a 60-bar window in `learn/training_set.py:FEATURE_NAMES` — `return_1/5/15/60, volatility_15/60, volatility_ratio, range_position, volume_ratio, trade_intensity, upper/lower_wick_ratio, close_to_vwap, momentum_consistency`. Only one of 23 `src/features/*.py` modules is imported by training: `sample_uniqueness` (label-overlap weighting). The other 22 (absorption, beta_to_btc, calendar_effects, consolidated_price, cross_sectional, feed_quality, fractional_differentiation, funding_basis, har_rv, kyle_lambda, meta_labelling, microprice, order_flow_imbalance, peg_monitor, price_divergence, realized_volatility, spot_perp_basis, staleness, term_structure, triple_barrier, universe_coverage, volatility_regime, volume_quality) are standalone, used by cost/validation/strategy/board code, not the LightGBM pipeline. | `src/learn/training_set.py:106-121,189-270`; `src/learn/train_segment_model.py:59-61` |
| Training cadence | `retrain_segment(segment, n_hours=14)`: reads last 14h of 1-min bars, fits direction + tail models, runs axis probes; designed to run on a schedule (docstring), distinct from online score updates (row: calibration). Refuses without crashing (`NothingLearned`) below 500 rows. Only `perp`/`spot` have bar-fitted datasets. | `src/learn/train_segment_model.py:413-474`; `src/learn/training_set.py:414-417` |
| Champion/promotion gate | Two purpose-built gates. **(a)** `validation/champion_promotion.evaluate_champion`: no-regression on `EDGE=accuracy-base_rate`; promotes only if `challenger_edge > incumbent_edge` (`DEFAULT_MIN_EDGE_IMPROVEMENT=0.0`, strict, ties→incumbent); must `beats_majority_class`. `evaluate_tail_champion` same logic on pinball loss at q=0.5. **(b)** `validation/promotion_gate.evaluate_for_promotion` (general/carry): `dsr >= 0.95` default, `available_years >= required_years` (MinBTL `≈2·ln(N)/SR²` yrs, floor 0.5), must beat naive baseline (α=0.05), `purge_retention >= 0.25`. PBO via CSCV (`backtest_overfitting.py`, `n_splits=16`), `MAX_PBO=0.50` in `promotion_pipeline.py`. SPA (Hansen test) α=0.05, `_MIN_PATHS=20`. DSR formula (Bailey & López de Prado) implemented in full. | `src/validation/champion_promotion.py:58-62,113-220`; `src/validation/promotion_gate.py:117-238`; `src/validation/deflated_sharpe.py:198-243`; `src/validation/backtest_overfitting.py:49-120`; `src/validation/promotion_pipeline.py:72` |
| Calibration | Binned reliability map, `learn/online_calibration.py`: `N_BINS=10`, needs `MIN_BIN_COUNT=30`/bin before trusting it (else raw score, `calibrated=False`). Updated online from realised outcomes via `observe()` every poll — separate from the LightGBM booster, which only updates on scheduled retrain. | `src/learn/online_calibration.py:69-73,86-127,171-192` |
| Ablation | `learn/axis_probes.probe_ablation`: replaces model decision with majority-class base rate, fails if `lift=model_acc-base_acc` not `>0`. `probe_randomisation`: swaps trained scores for random ones, requires decisions differ by `>0.05`. Both auto-run at end of every `retrain_segment()`. | `src/learn/axis_probes.py:85-142`; `src/learn/train_segment_model.py:465-495` |
| Abstention | Conformal-prediction based, not verbalised. `should_abstain`: non-conformity `=1-max(p,1-p)`, abstains if `> quantile()` of last `CONFORMAL_WINDOW=500` scores at `DEFAULT_ALPHA=0.35` (65% target coverage); needs `MIN_CONFORMAL_SCORES=50` or abstains on everything. Coverage audited (`realised_coverage >= promised-0.10`). Arbiter also abstains on `MARGIN_TOO_THIN`, `BELOW_CONFIDENCE_FLOOR`, `NEGATIVE_EXPECTANCY`, `TAIL_TOO_WIDE`, `NO_PROPOSAL`. | `src/learn/online_calibration.py:73-83,130-213`; `src/segment/arbiter.py:54-59,111-147` |
| Regime handling | `features/volatility_regime.compute_volatility_regime`: per-(venue,symbol) volatility decile (1-10), explicitly "no verdicts". Consumed by `ledger_meta_model` as a caller-supplied key, not wired into segment training/brain decisions. `learn/axis_probes.probe_out_of_regime` is **hard-coded to always FAIL** ("eight non-contiguous days of one regime" in the archive). | `src/features/volatility_regime.py:103-131`; `src/learn/axis_probes.py:189-203` |
| Research/ingestion, LLM use | Neither present — see NOT PRESENT below. |  |
| Memory of past trades | `learn/belief.py`: every decision wrapped in `Belief` with mandatory `half_life_ns` and `epistemic_class` (only `OBSERVED` `may_size()`) — per-decision epistemic wrapper, not itself trained. `models/ledger_meta_model.fit`: ridge-logistic over the trial ledger (family×regime), `MIN_TRIALS_TO_FIT=120`, forward-chronological split, but no import found from `segment/`/`perp/`/`spot/`/`train_segment_model.py` — not wired into runtime decisions. `online_calibration.observe()` is the mechanism that actually feeds realised outcomes back live. | `src/learn/belief.py:63-70,126-224`; `src/models/ledger_meta_model.py:77-86,225-277`; `src/learn/online_calibration.py:171-192` |
| Universe-wide scanning | Per `segment/bot_registry.py` docstring (measured 2026-08-18): perp 570 perpetuals, spot 1,361 pairs, dated 48 contracts, options 1,442. Admission gates per segment (perp: `NOT_PERPETUAL/TOO_LITTLE_HISTORY/WENT_QUIET/ILLIQUID`; spot: `NEVER_QUOTED/NO_TWO_SIDED_QUOTE/QUOTE_STALE/TAPE_TOO_THIN`). **Training is pooled cross-sectional** (~1,698 symbols pooled, ~500 bars/symbol each — insufficient alone), with cross-sectional normalization (returns/ratios/ranks, never raw price). **Inference is per-symbol independent** — each brain call takes one frame for one venue/symbol; `features/cross_sectional.py` computes true cross-sectional ranks but isn't wired into training. `validation/breadth.effective_breadth` (eigenvalue participation ratio, `COLLAPSE_BAND=(5,15)`, `CLUSTERING_LIMIT=2.0`) measures true independent-bet count but is used on the carry strategy, not segment brains. | `src/segment/bot_registry.py:29-34`; `src/perp/tradable_universe.py:18-52`; `src/spot/tradable_universe.py:13-24`; `src/learn/training_set.py:9-18`; `src/validation/breadth.py:26-37,292-296` |

**NOT PRESENT** (searched, not found):
- External research/news/alt-data ingestion — grepped `\bnews\b`, `sentiment`, `twitter`, `reddit`, `alternative.?data`, `web.?scrap`, `rss`, `nlp` across `src/`; only exchange price/orderbook/funding feeds exist.
- LLM usage — grepped `openai`, `anthropic`, `claude` (as API), `gpt-`, `llm`, `chatgpt`, `gemini`, `langchain` across `src/learn`, `src/models`, `src/validation`, `src/strategy`, `src/features`, `src/segment`, `src/perp`, `src/spot`. Zero runtime calls; only a hypothetical mention in `src/learn/axis_probes.py:151` ("A future brain that consults an LLM... would fail here") and unrelated references to the `CLAUDE.md` doc file.
- xgboost/sklearn-model/PyTorch/TensorFlow — only LightGBM + hand-written ridge-logistic (IRLS)/random-walk baselines.
- Non-`sample_uniqueness` `src/features/*.py` modules wired into segment LightGBM training — confirmed by import grep on `training_set.py`/`train_segment_model.py`.

## Axis 3 — architecture

| mechanism | what the code does | evidence |
|---|---|---|
| Process model | One OS process per segment, launched by a bash supervisor loop (`python -m segment.live_engine --segment "$SEGMENT" & wait`), restarted with backoff (<15s runtime → 15s sleep, else 2s). `--segment` required, no default. Per-segment `OFF` file pauses without touching capture. No `subprocess`/`multiprocessing` spawning inside `src/segment` or `src/ops` (only unrelated `subprocess.run` for `gcloud`/`sudo -n true`). | `scripts/segment_bot_supervisor.sh:71-91`; `src/segment/live_engine.py:1-4,136-171,1025-1033` |
| Data store | Parquet partitioned by `availability_hour` (hive key `availability_hour=<YYYY-MM-DDTHH>`, UTC floored), plus `symbol=<S>` second level except for `WHOLE_UNIVERSE_DATASETS` (funding, funding_reconstructed, option_chain, bars_60000000000ns, bars_reconstructed_60000000000ns, book, dated_futures, exchange_reserves) which partition by hour only. Files `part-<snapshot_id>.parquet`, ZSTD, snapshot id = first 16 hex of SHA-256 over sorted per-file digests. Append-only: `PartitionExistsError` on overwrite attempt; writes go to temp file then `os.replace`+fsync. Cited cost of the old symbol-level layout: 1,892 fragments / 26,015 rows in one sealed funding hour, 30.6 min to read an 850MB dataset. | `src/store/parquet_partition.py:48-79,211-233,339-443` |
| Journal/WAL + hash chain | **No hash-chained journal exists.** Journals (`fills-*.ndjson`, `decisions-*.ndjson`, capture ledger `events.ndjson`) are plain append-only NDJSON, fsynced per write, rows carry no prev-hash field. `parquet_partition.compute_snapshot_id` hashes file *content* (SHA-256) for dedup/identity, not a chain of records. Capital pool ledger (`pool.ndjson`) also unhashed, flock-guarded. | `src/capture/capture_ledger.py:20-27,40-68`; `src/store/parquet_partition.py:303-328`; `src/segment/capital_pool.py:65-76,162-176` |
| Probes / status wall | `statuswall/segment_probes.py`: ~30 probes reading only running-system artifacts (heartbeat JSON, `STALE_AFTER_S=120.0`; bounded journal tails `TAIL_ROWS=400`/`TAIL_BYTES=4_000_000`; model registry; universe files) — never a constant or test result. States `OK/PARTIAL/DEGRADED/FAILING/NOT_MEASURED`, worst-first. Served by `board_server.py`: stdlib `http.server`, HTTP Basic auth (`hmac.compare_digest`), bound `127.0.0.1:8787` only, no directory listing, `Cache-Control: no-store`, refuses to start unless credentials file is mode 0600. | `src/statuswall/segment_probes.py:60-79,138-226`; `src/statuswall/evidence.py:31-56`; `src/statuswall/board_server.py:76-183` |
| Kill switch / halt | Two mechanisms. **Venue halt** (`ops/venue_halt.py`): halts only on `corrupting_non_gap>0` (limit 0, one event triggers); silence-based halting explicitly disabled (`_SILENCE_LIMIT=None`, caused false positives — 1,845 "silent" events on a healthy binance/2,123-symbol run). Sticky: clears only after 30 min (`_RECOVERY_DWELL_NS`) continuous health. Unknown venue fails closed (`is_tradeable()→False`). **Process kill switch** (`ops/watchdog.py.trip`): writes `KILLED.json` first, then attempts credential denial (moves aside age/sops identity), then attempts iptables/nft network drop — measured **unavailable on this host** (no passwordless sudo, no iptables/nft installed, `network_dropped` always `False`), then `os.kill(pid, SIGKILL)`. | `src/ops/venue_halt.py:41-49,70-177`; `src/ops/venue_health_watch.py:63-99`; `src/ops/watchdog.py:47,69-193` |
| Resource governance | `ops/rate_budget.py`: per-venue weight-based token bucket, file-backed (`rate_budget_<venue>.json`), `fcntl.flock`-guarded across processes. `KNOWN_CAPACITY`: binance 1600/min (fapi), binance-spot 4000/min, hyperliquid 1000/min ("a guess"). Refill = `capacity/60.0`/sec (smooth). `try_spend(weight)` never blocks, returns bool. `note_rate_limited`: `blocked_until=clock()+retry_after`, tokens zeroed. Weight not request-count spent (e.g. depth snapshot weight 50 vs `premiumIndex` weight 1). | `src/ops/rate_budget.py:44-149` |
| Segment isolation | 4 segments (perp/spot/dated/options), each own OS process, venue, brains, feed, journal dir. Capital isolation: `GOVERNED_SEGMENTS=("perp","spot")` only. One shared `capital.json` declares `portfolio_usdt`+`per_bot_cap_fraction`; `CapitalPool` (flock, replayed from ledger every call, never cached) enforces `cap_for(segment)=portfolio*fraction` as a per-bot ceiling, refuses with `POOL_EXHAUSTED`/`BOT_CAP_REACHED`/`BELOW_MIN_MARGIN`/`ALREADY_RESERVED`. Startup sweep releases only the calling bot's own orphaned reservations. `bot_registry.segment_bot(segment)` builds distinct brain objects per segment; a probe checks no two segments share venue/brain set. | `src/segment/capital_declaration.py:65,124-137`; `src/segment/capital_pool.py:1-52,205-305`; `src/statuswall/segment_probes.py:60,830-855` |
| Config | **No central config module/class** (`find src -iname '*config*'` empty, no `class Config`). Per-domain: capital budget in one JSON file, validated by `DeclarationReloader` (polls by mtime, rejects wholesale on any bad field, no partial acceptance, numeric fields must be JSON strings not floats). Board credentials in `~/.config/boards/credentials` (mode 0600 enforced), env override `BOARDS_CREDENTIALS_FILE`. Rate-budget capacities are Python constants. Supervisor scripts read env vars (`CAPTURE_ROOT`, `SEGMENT_PYTHON`). No YAML config referenced from `src/*`. | `src/segment/capital_declaration.py:2-3,60-89,168-399`; `src/statuswall/board_server.py:28-60`; `scripts/segment_bot_supervisor.sh:24-25` |
| Tests | **142** files matching `find tests -name 'test_*.py' -not -path '*/__pycache__/*'` (verified directly by this task too). | shell output |
| LOC per top-level `src` dir | `capture` 6,797 · `cost` 985 · `dated` 342 · `execution` 689 · `features` 6,059 · `integrity` 543 · `learn` 2,265 · `live` 1,101 · `models` 2,522 · `ops` 1,163 · `options` 402 · `paper` 3,174 · `perp` 726 · `plan` 703 · `risk` 1,243 · `segment` 3,967 · `spot` 269 · `statuswall` 8,525 · `store` 5,867 · `strategy` 911 · `validation` 2,869 (largest: statuswall, capture, features, store; smallest non-empty: spot, dated, options). | `wc -l` loop, verified directly by this task |

**NOT PRESENT** (searched, not found):
- `docs/ARCHITECTURE.md` — does not exist on disk (confirmed by this task too: `ls docs/ARCHITECTURE.md` → no such file). Yet `ops/rate_budget.py:3-6`, `ops/venue_halt.py:3-4,21-23,29`, `ops/watchdog.py:3-4,10` all cite it by name as the design source for rate-limit/halt/watchdog behaviour — the design intent lives only in those modules' own docstrings, not in a standalone doc.
- Hash-chained journal/WAL with `prev_hash`/`chain_hash` field — searched `capture/capture_ledger.py`, `integrity/*`, `segment/live_engine.py`, `segment/capital_pool.py`; none chain records by hash.
- Central `Config`/settings class or module — `find src -iname '*config*'` and grep for `import config` both empty.
- Working firewall-based network kill (iptables/nft execution) — `ops/watchdog.py:159-168` states this is unimplemented/untested on this host (no sudo, no iptables/nft); code only measures and records the failure, does not perform the block.

---
*Compiled from three read-only research passes over this checkout on 2026-08-20. No file in `trading-system` was modified and no bot was run to produce this document.*
